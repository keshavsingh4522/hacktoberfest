#include <bits/stdc++.h>
using namespace std;
int main() {

    //using char
   /* char ch[3]={'b','c','a'};
    sort(ch,ch+3);

    for(int i=0;i<3;i++)
    cout<<ch[i]<<" ";*/

    //using string
    string s="bcdaf";
    sort(s.begin(),s.end());

    cout<<s;
}
