/* BUBBLE SORT IN C LANGUAGE*/
#include<stdio.h>
int main(){
int i,j;
int a[]={2,5,4,8};
for(i=0;i<n;i++)
{
for(j=0;j<n-1-i;j++)
{
if(a[j]>a[j+1])
{
temp=a[j];
a[j]=a[j+1];
a[j+1]=temp;
}
}
}
for(i=0;i<n;i++)
printf("%d",a[i]);
}
