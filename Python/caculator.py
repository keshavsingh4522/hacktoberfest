# Get operation Multiply Or Divied or subtract or Add #
op = input('M for Multiply D For Divison A for Addition S for subtraction')
# Get operation Multiply Or Divied or subtract or Add #

# Get the first Number #
n1 = input('input the first number')
# Get the second number #
n2 = input('input the second number')

#Additon operation #
if op == 'A': # If op = a (additon)
  # get sum and print #
 print(int(n1+n2))
if op == 'S': #if op = S (Subtract)
  # get sum and print #
 print(int(n1-n2))
if op == 'M': # If op = m (Multiply)
  # get sum and print #
 print(int(n1*n2)) 
if op == 'D': # If op = d (Division)
  # get sum and print #
 print(int(n1/n2))
##########################
#           END          #
